dist/
node_modules/
package-lock.json
playwright-report/
test-results/
rooms*.png

# yamllint --strict owns these, and the two tools disagree: prettier collapses
# the two spaces before an inline comment that yamllint requires. One formatter
# per file, so the workflows belong to the linter that also validates them.
.github/
